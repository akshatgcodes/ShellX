# ShellX — landing page

A single-page Next.js (App Router + TypeScript + Tailwind CSS) showcase for
[ShellX](../shellx.c), a minimal POSIX shell written from scratch in C.

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
